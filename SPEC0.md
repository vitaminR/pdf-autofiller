# SPEC0 — pdf-autofiller

## Repo structure (expected)

- `mappings/` for mapping definitions
- `inbox/` for incoming PDFs/data
- Core scripts in repo root (CLI helpers)

## Conventions

- Keep generated outputs out of git.
- Prefer deterministic mapping generation.
